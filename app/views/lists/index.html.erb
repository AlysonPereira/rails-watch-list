<div class="container"><h1>List</h1></div>

<% @lists.each do |list| %>
    <p>
      <div class="container"><%= link_to list.name, list_path(list), class: 'text-decoration-none' %></div>
    </p>
<% end %>

<div class="container"><%= link_to 'Add List', new_list_path, class:'btn btn-sm btn-success' %></div>
